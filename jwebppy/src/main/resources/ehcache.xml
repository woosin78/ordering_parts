<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.ehcache.org/v3"
        xmlns:jsr107="http://www.ehcache.org/v3/jsr107"
        xsi:schemaLocation="
            http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core-3.0.xsd
            http://www.ehcache.org/v3/jsr107 http://www.ehcache.org/schema/ehcache-107-ext-3.0.xsd">
<!-- config : XML 구성의 루트 요소이다. --> 


	<cache-template name="platform">
		<!-- 캐시 만료기간 설정 -->
		<expiry>
			<!-- unit : nanos, micros, millis, seconds, minutes, hours, days -->
			<ttl unit="days">1</ttl>
		</expiry>
		
		<!-- listeners는 Cache의 리스너를 등록하는 요소이다.-->
		<listeners> 
			<listener>
				<class>org.jwebppy.platform.core.cache.CacheEventLogger</class>
				<event-firing-mode>ASYNCHRONOUS</event-firing-mode>
				<event-ordering-mode>UNORDERED</event-ordering-mode>
				<events-to-fire-on>CREATED</events-to-fire-on>
				<events-to-fire-on>EXPIRED</events-to-fire-on>
			</listener>
		</listeners>

		<resources> <!-- resources는 캐시 데이터의 저장 공간과 용량을 지정한다. 만약 힙 메모리만 사용한다면 <heap> 요소만으로 대체할 수 있다.  -->
			<heap unit="entries">100</heap> <!-- heap은 JVM 힙 메모리에 캐시를 저장하도록 세팅하는 요소이다. --> 
			<offheap unit="MB">10</offheap> <!-- offheap은 JVM 힙 메모리 외부의 메모리에 캐시를 저장하도록 세팅하는 요소이다. -->
		</resources>
	</cache-template>
	
	<cache alias="Language" uses-template="platform">
	<!-- key-type>java.lang.Long</key-type>  key-type 요소는 Cache 인스턴스에 저장될 캐시의 키의 FQCN을 지정한다. 즉, 키의 타입을 명시해주면 된다. 기본 값은 java.lang.Object 이다. -->
	<!-- value-type>java.math.BigDecimal</value-type>  value-type 요소는 Cache 인스턴스에 저장된 값의 FQCN을 지정한다. 기본 값은 java.lang.Object 이다. -->
	</cache>
	<cache alias="User" uses-template="platform"/>
	<cache alias="Content" uses-template="platform"/>



</config>