<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.jwebppy.portal.iv.hq.parts.promotion.mapper.PromotionItemMapper">

	<insert id="insert" parameterType="PromotionItemEntity" useGeneratedKeys="true" keyProperty="piSeq">
		<selectKey keyProperty="piSeq" resultType="Integer" order="BEFORE">
			select ep_pi_seq.nextval from dual
		</selectKey>
	
		insert into EP_PROMOTION_ITEM (pi_seq, p_seq, material_no, fg_delete, reg_username, reg_date) 
		values (#{piSeq}, #{pSeq}, #{materialNo}, 'N', #{regUsername}, #{regDate})
	</insert>
	
	<update id="updateFgDelete" parameterType="PromotionItemEntity">
		update EP_PROMOTION_ITEM set
			fg_delete='Y'
		where
			p_seq=#{pSeq}
			and fg_delete='N'
	</update>
	
	<select id="findPromotionItems" parameterType="int" resultType="PromotionItemEntity">
		select
		a.*
		from
		EP_PROMOTION_ITEM a
		where
		a.p_seq=#{pSeq}
		and a.fg_delete='N'
	</select>
	
	
</mapper>